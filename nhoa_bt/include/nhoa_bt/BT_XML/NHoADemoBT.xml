<?xml version="1.0"?>
<root main_tree_to_execute="NHoADemoBT">
    <!-- ////////// -->
    <BehaviorTree ID="NHoADemoBT">
        <Sequence>     
            <SubTree ID="T1"/> 
        </Sequence>
    </BehaviorTree>
    <!-- ////////// -->
    <BehaviorTree ID="T1">
        <Sequence>        
            <SetBlackboard output_key="_motion_name" value="look_around"/>
            <RetryUntilSuccessful num_attempts="30">
                <Fallback>
                    <Condition ID="IsUserDetected"/>
                    <Inverter>
                        <Action ID="ExecuteMotion" motion_name="{_motion_name}"/>
                    </Inverter>
                </Fallback>
            </RetryUntilSuccessful>
        </Sequence>
    </BehaviorTree>
    <!-- ////////// -->
    <!-- <BehaviorTree ID="T2">
        <Sequence>        
            <RetryUntilSuccessful num_attempts="30">
                <Fallback name="UserDetection">
                    <Condition ID="IsUserDetected"/>
                    <SetBlackboard output_key="_motion_name" value="look_around"/>
                    <Action ID="ExecuteMotion" motion_name="{_motion_name}"/>
                </Fallback>
            </RetryUntilSuccessful>
        </Sequence>
    </BehaviorTree> -->
    <!-- ////////// -->
    <TreeNodesModel>
        <Action ID="ExecuteMotion">
            <input_port name="motion_name" type="std::string"/>
        </Action>
        <Action ID="DrawUserAttention">
            <input_port name="_iteration" type="std::string"/>
            <output_port name="motion_name_" type="std::string"/>
            <output_port name="voice_cmd_" type="std::string"/>
            <output_port name="iteration_" type="std::string"/>
        </Action>
        <Condition ID="IsUserDetected"/>
        <Condition ID="IsUserEngaged"/>
        <Condition ID="IsUserEngaging"/>
        <SubTree ID="T1">
            <!-- <input_port default="false" name="__shared_blackboard">If false (default), the Subtree has an isolated blackboard and needs port remapping</input_port>
            <input_port default="move_group" name="_move_group"/> -->
        </SubTree>
    </TreeNodesModel>
    <!-- ////////// -->
</root>

